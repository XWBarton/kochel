POSTGRES_USER=kochel
POSTGRES_PASSWORD=changeme
POSTGRES_DB=kochel

# Host path to your music library. Mounted read-only into the app container —
# Phase 1 only reads/streams, it never writes into your library.
MUSIC_LIBRARY_PATH=./dev-library

APP_PORT=8000
CORS_ORIGINS=*
WEB_PORT=3000
